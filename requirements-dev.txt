-r requirements.txt

pytest==8.3.4
httpx==0.28.1
ruff==0.9.3
black==24.10.0
mypy==1.14.1

# Test-only: generates minimal PDF fixtures for extractor tests, so tests
# don't depend on a committed binary sample file.
fpdf2==2.8.2
